# ROA Programme Header Contract v1

The root README of every materialized programme repository starts with two badges, a machine-readable marker and one bounded membership paragraph before the repository H1.

## Required fields

1. canonical programme badge linked to `Luke883i/academics`;
2. repository-specific programme-role badge;
3. marker `roa-programme-header:v1` with hub, framework and role;
4. canonical programme name;
5. explicit theoretical anchor `Luke883i/ROA`;
6. explicit local-authority boundary;
7. explicit non-entailment boundary: membership alone does not establish derivation, conformance, empirical validation or production readiness.

Mutable release/version/HEAD state is forbidden in the shared programme header.

## Global falsification families

The v1 campaign mutates: membership badge removal; hub redirection; role drift; ROA-anchor removal; programme-name drift; local-authority removal; claim-boundary removal; validation laundering; mutable state injection; and displacement of badges below a title. Baseline: 11/11 generated headers accepted. Mutation campaign: 110/110 mutated cases rejected, 0 survivors.

Programme membership is topology, not scientific proof.
